name: 'YAML Lint'

on:
  pull_request:
    types:
      - opened

jobs:
  yamllint:
    name: 'YAMLLint'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: yaml-lint
        uses: ibiqlik/action-yamllint@v3
