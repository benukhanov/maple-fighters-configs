name: 'YAML Lint'

on:
  pull_request:
    types:
      - opened

jobs:
  yamllint:
    name: 'YAMLLint'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@master
      - name: 'Yamllint'
        uses: karancode/yamllint-github-action@master
